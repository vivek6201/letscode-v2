import React from "react";

export default function AddTutorialContent({
  tutorialId,
  parentContentId,
}: {
  tutorialId: number;
  parentContentId: number;
}) {
  return <div>AddTutorialContent</div>;
}
