import React from "react";

export default function page({ params }: { params: { moduleId: string; tutorialId: string } }) {
  const tutorialId = params.tutorialId;
  const rest = params.moduleId;

  

  return <div></div>;
}
